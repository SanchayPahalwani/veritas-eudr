# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
coverage.xml

# Env / secrets
.env
.env.*
!.env.example

# OS
.DS_Store

# Local Claude Code harness config
.claude/

# Large/derived data: committed fixtures live under tests/fixtures/ and
# web/basemap/ explicitly; raw downloaded granules do NOT enter git.
data/raw/
data/cache/
*.zip

# Terraform local state (deploy/ ships HCL + validate/fmt output, never state)
deploy/.terraform/
deploy/*.tfstate
deploy/*.tfstate.*
deploy/.terraform.lock.hcl

# Whisp live-call cache that is not the committed fixture set
tests/fixtures/whisp/_live_*.json
